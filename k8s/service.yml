apiVersion: v1
kind: Service
metadata:
  name: lrjmpzzl
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lrjmpzzl"
    port: 8080
  selector:
    app: lrjmpzzl
